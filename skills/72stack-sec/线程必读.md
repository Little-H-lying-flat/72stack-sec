# 线程必读（从 ~/.grok/skills/skill/线程必读.md 复制到任务 `{短名}_dig/线程必读.md`，填任务根）

迭代见磁盘 `~/.grok/rules/hunt-iter.md`。正式报告只认 `~/.grok/rules/vuln-report-format.md`。进站认 `dig-scope` §4。调度细则只认同目录 `主控调度.md`（线程不调度、不发码；线程只读本文件）。

- 本目录为 SRC 挖洞主 skill；可按需叠用其它已安装 skill（如小程序相关）。立法仍认 `~/.grok/rules`。对外产品名见 `NAMING.md`（72stack-sec）。
- CORS 不挖。禁止 logout / signout / revoke。禁止改短表和知识库。
- FOFA 禁止进线程。**一人一轨一站**，不要自己换站、不要自己改轨。
- spawn **第一行** `本轨=未登录` / `本轨=未登录一眼` / `本轨=有会话`。只做这一轨。
- **约束测试红线**认 skill `SKILL.md` 该节：真实数据/注入出数 ≤5 组；禁止写操作、WebShell、真跑系统命令、扫描器批量/爆破/DoS、内网横向、社工/打扰真实用户；AK/SK/kube 禁止自行拿去进业务。清单内 curl 探针不是扫描器。密钥实值只进 `报告/`。

禁止跑 `profile_get.py` / `sms_code.py` / `email_code.py` / `auth_flow.py` / `pending_from_done.py`。禁止改 `资产/pending_register.md`。

## 本轨=未登录

主业：§4.0 → JS 到 `js/{host}/anon/` → 未登录例外 + 有差分面四件套 + 认证口清单。打开是登录页：找业务 API + 发码/提交 API。看见登录页不是换资产。

DONE → `{dig}/{host}/DONE_anon.md`。清单 → `endpoints.md` + `matrix.md`。

**必写缺号行：**

`缺号：干净|放弃|无HTTP口；发码=<METHOD URL或无>；提交=<METHOD URL或无>；通道=sms|email|无；sid=<或无>；盾=无|滑块|实名|SSO；原因=…`

- **要发码 ≠ 放弃。** 发码/提交 API 写 METHOD+完整 URL，标干净。
- **登录页 URL 不是干净口。** 抠不出 API → `无HTTP口`。滑块/实名/别人的 IdP 本身 → `放弃`。
- 禁止写 cookie、禁止假装有会话、禁止写 `DONE_auth`。
- 瘦壳一眼证伪即可。肥面有干净口：写缺号行，**禁止**自己标 covered。

## 本轨=未登录一眼

只找发码/提交 API + 点未登录例外，写 `DONE_anon` 缺号行即停。禁止全矩阵、禁止对着登录码灌四件套、禁止写 cookie。

## 本轨=有会话

spawn 会带 cookie 路径和身份（有店铺|空户|待确认）。用法：`curl -b {dig}/{host}/session.cookie`；仍 109 把 name=value 拼 `Cookie:` 头。禁止再发码。

**换票 result=1 ≠ 有业务身份。** spawn 写「空户」或「待确认」→ 第一枪打身份口；仍空则打资质/开通跳步，不要对着 109 换 id。写「有店铺」→ 对象图/换 id。

主业：先读 `js/{host}/anon/`；带着本 sid cookie GET 业务首页，只补新 chunk 到 `js/{host}/{sid}/`。懒加载文件名在主包里，**禁止靠点菜单当抽干**。路由表 / hidden path 没画在页面也直接打 API。细节 `知识库/js-reverse-guide.md`「登录后懒加载」。§4.0 用这批包说清这摊。spawn 写了「有号挖」时未登录例外仍点；默认挖X 未登录轨已点过则不必重复。

DONE → `{dig}/{host}/DONE_auth.md`。清单 → `endpoints_auth.md` + `matrix_auth.md`。必写 `身份=有店铺|空户` + `资质口=<METHOD URL或无>`。禁止写缺号行催新号。

## 两轨都要

1. 先读 `知识库/打穿短表.md` 开场几枪，立刻回本站清单。
2. 同闸 sibling 一眼差分。中危同一对象先升链。打成了按 format 落 `报告/`。
3. 有高危：本轨 DONE 写拟进/拟补/不进（中危不拟进）。

## DONE 最低字段

host；本轨；§4.0；肥/瘦；清单/钥匙；矩阵（本轨该做的：未登录例外 / 认证口 / 四件套 / 换 id 或资质）；报告或无洞；未登录轨 **缺号行**；登录轨 **身份行**；拟进/拟补/不进（有高危时）；回灌 R（优质才写，不要自己 FOFA）。

## suspects（两轨都要）

- 本 host 落 `{dig}/{host}/suspects.md`（模板见 skill `references/templates/suspects-template.md`）。瘦壳可一行 `N/A 瘦壳`+原因。
- 先语义疑问再确认；禁止无 S-xx 映射就按模块整站盲打。
- DONE 最低字段增加：`suspects=已写|N/A瘦壳`；覆盖率自检未勾且无 N/A → 不要暗示主控可 covered。
- 打成中危+：回单里用一句话写「建议主控回灌盲区库」的语义疑问（勿写 payload）。

