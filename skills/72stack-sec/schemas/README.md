# schemas（自用 → 72 子集）

真源仍是任务根文件；这里只描述形状。

- 必填子集认 `workspace/schema-mapping-自用-to-72.md` + 蚂蚁钉口径
- 校验 FAIL → **WARN**，不挡真空翻种 / `p2` / `mark_covered`
- FOFA 挂 / 0 NEW ≠ `closed`
- `schema_warn.py` 后做

四张：`queue-item` / `asset` / `finding` / `baseline-diff`
